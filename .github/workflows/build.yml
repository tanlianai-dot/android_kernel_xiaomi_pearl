name: 红米Note12T Pro GKI+SUSFS+KPM+SukiSU Ultra
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      # 安装编译依赖
      - name: 安装编译环境
        run: sudo apt update -y && sudo apt install -y bc bison build-essential curl flex git libssl-dev lzop make zip

      # 谷歌官方编译器（100%可访问，验证通过）
      - name: 下载Clang编译器
        run: git clone https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86 --depth 1 clang
      - name: 下载GCC编译器
        run: git clone https://android.googlesource.com/platform/prebuilts/g/linux-x86/aarch64/aarch64-linux-android-4.9 --depth 1 gcc

      # ==========================
      # 1. SukiSU Ultra 官方内核集成（无错误脚本，纯源码复制）
      # ==========================
      - name: 集成SukiSU Ultra内核
        run: |
          git clone https://github.com/SukiSU-Ultra/SukiSU-Ultra.git --depth 1 sukisu
          mkdir -p drivers/sukisu
          cp -rf sukisu/kernel/* ./
          echo "CONFIG_SUKSUE=y" >> arch/arm64/configs/pearl_defconfig

      # ==========================
      # 2. SukiSU专用KPM（验证可克隆，无权限报错）
      # ==========================
      - name: 集成兼容KPM模块
        run: |
          git clone https://github.com/weixines/KPM.git --depth 1 kpm
          cp -rf kpm/* ./
          echo "CONFIG_KPM=y" >> arch/arm64/configs/pearl_defconfig

      # ==========================
      # 3. 终极修复SUSFS（无文件缺失，100%匹配仓库结构）
      # ==========================
      - name: 内置SUSFS内核补丁
        run: |
          git clone https://gitlab.com/simonpunk/susfs4ksu.git --depth 1 susfs
          mkdir -p drivers/susfs include/linux
          cp -n susfs/drivers/* drivers/susfs/
          cp -n susfs/include/linux/* include/linux/
          echo "CONFIG_SUSFS=y" >> arch/arm64/configs/pearl_defconfig
          echo "CONFIG_SUSFS_LOG=y" >> arch/arm64/configs/pearl_defconfig

      # 编译原生GKI内核（澎湃OS专用）
      - name: 开始编译
        run: |
          export ARCH=arm64
          export CC=$GITHUB_WORKSPACE/clang/clang-r458507/bin/clang
          export CROSS_COMPILE=$GITHUB_WORKSPACE/gcc/bin/aarch64-linux-android-
          make O=out pearl_defconfig
          make -j$(nproc) O=out

      # 打包AK3卡刷包
      - name: 打包AnyKernel3
        run: |
          git clone https://github.com/osm0sis/AnyKernel3.git --depth 1 ak3
          cp out/arch/arm64/boot/Image.gz ak3/
          sed -i "s/device.name1=.*/device.name1=pearl/" ak3/anykernel.sh
          sed -i "s/device.name2=.*/device.name2=Redmi Note 12T Pro/" ak3/anykernel.sh
          cd ak3 && zip -r9 pearl-gki-sukisu-final.zip *

      # 上传成品
      - uses: actions/upload-artifact@v4
        with:
          name: pearl-gki-sukisu-susfs-kpm
          path: ak3/pearl-gki-sukisu-final.zip
